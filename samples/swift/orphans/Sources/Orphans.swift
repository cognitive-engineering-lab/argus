@main
public struct Orphans {
    public static func main() {
        let mySet: Set<U> = []
        insert(U.y, mySet)
        insert(U.x, mySet)
        insertp(U.x, mySet)
        print(mySet)
    }
}
